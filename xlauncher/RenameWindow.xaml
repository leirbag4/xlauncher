<Window x:Class="xlauncher.RenameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename" Height="180" Width="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True">

    <Border Background="#1E1E1E" CornerRadius="10" BorderBrush="#333333" BorderThickness="1">
        <StackPanel Margin="20">
            <TextBlock Text="Renombrar Acceso Directo" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>

            <TextBox x:Name="NameInput"
                     FontSize="16"
                     Background="#252526"
                     Foreground="#8A2BE2"
                     FontWeight="Bold"
                     BorderBrush="#333333"
                     Padding="5"
                     CaretBrush="White"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Cancel" Width="80" Height="30" Click="Cancel_Click" 
                        Background="#333" Foreground="White" BorderThickness="0" Margin="0,0,10,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Content="Save" Width="80" Height="30" Click="Save_Click" 
                        Background="#8A2BE2" Foreground="White" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>